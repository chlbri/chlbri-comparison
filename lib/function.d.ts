/**
 *
 * @param args (only defined functions, not arrow functions)
 * @returns true if all functions have the same shape.
 */
export declare function compareFunctions(...args: ((...args: any) => any)[]): boolean;
